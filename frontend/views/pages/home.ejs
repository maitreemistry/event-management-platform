<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>THE GD HEADS - Event Management Platform</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f9;
            margin: 0;
            padding: 0;
            color: #333;
        }

        header {
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            text-align: center;
        }

        .container {
            max-width: 1200px;
            margin: 20px auto;
            padding: 0 15px;
        }

        .home-container {
            background-color: #fff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }

        .home-container h1 {
            font-size: 2.5em;
            color: #4CAF50;
        }

        .home-container p {
            font-size: 1.2em;
            color: #555;
            line-height: 1.6;
        }

        .events-list {
            margin-top: 40px;
        }

        .events-list h2 {
            font-size: 2em;
            color: #333;
        }

        .event-card {
            background-color: #fff;
            padding: 20px;
            margin: 10px 0;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .event-card h3 {
            font-size: 1.8em;
            color: #333;
            margin-bottom: 10px;
        }

        .event-card p {
            font-size: 1.1em;
            color: #666;
            margin-bottom: 5px;
        }

        .buttons a {
            display: inline-block;
            padding: 10px 20px;
            margin: 10px 0;
            background-color: #4CAF50;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .buttons a:hover {
            background-color: #45a049;
        }

        footer {
            text-align: center;
            padding: 20px;
            background-color: #4CAF50;
            color: white;
            margin-top: 40px;
        }
    </style>
</head>
<body>
    <header>
        <h1>THE GD HEADS TEAM welcomes you to the Event Management Platform!</h1>
    </header>

    <div class="container">
        <div class="home-container">
            <p>Your one-stop solution for managing and attending events. Browse through our upcoming events and register for the ones you are interested in!</p>

            <div class="events-list">
                <h2>Upcoming Events:</h2>
                <% if (events && events.length > 0) { %>
                    <ul>
                        <% events.forEach(event => { %>
                            <li class="event-card">
                                <h3><%= event.name %></h3>
                                <p><strong>Date:</strong> <%= event.date %></p>
                                <p><strong>Location:</strong> <%= event.location %></p>
                                <p><strong>Description:</strong> <%= event.description %></p>
                                <!-- <a href="/events/<%= event.id %>" class="button">View Event</a> -->
                            </li>
                        <% }) %>
                    </ul>
                <% } else { %>
                    <p>No upcoming events found.</p>
                <% } %>
            </div>
        </div>

        <div class="buttons">
            <a href="/events/create" class="button">Create Event</a>
            <a href="/feedback" class="button">Provide Feedback</a>
        </div>
    </div>

    <footer>
        <p>&copy; 2025 Event Management Platform. All rights reserved.</p>
    </footer>
</body>
</html>
